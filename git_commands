git init  - initialize an empty local repo
git add . - stage changes 
git commit -m"commit message" -takes a snapshot of the current state of the repo
git log - shows commit history 
git status - shows the current state/branch 
git remote add <remote-repo-name> <remote-repo-link>  -links your local repo to your remote repo
git remote -v - used for visualization
git push -u <remote-repo-name> <remote-branch>  - uploads to remote repo
git pull - this downloads from remote repo 

